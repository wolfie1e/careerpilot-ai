"use client";
import { useState, type ChangeEvent } from "react";
import { Archive, Download, ExternalLink, FolderKanban, Plus, Search, Star, Trash2, Upload } from "lucide-react";
import { toast } from "sonner";
import { CopyButton } from "@/components/shared/CopyButton";
import { useLocalStorage } from "@/hooks/useLocalStorage";
import type { PlannerTask } from "@/lib/career-planner";
import { LOCAL_STORAGE_KEYS } from "@/lib/constants";
import { downloadCsv, downloadJson } from "@/lib/export-utils";
import {
  PORTFOLIO_PROJECT_PRIORITIES,
  PORTFOLIO_PROJECT_STATUSES,
  createPortfolioProject,
  isPortfolioProjectOverdue,
  mergePortfolioProjects,
  nextPortfolioDeadline,
  portfolioDueSoonCount,
  portfolioAverageHours,
  portfolioMissingNextActionCount,
  portfolioMissingEvidenceCount,
  portfolioUnscheduledCount,
  portfolioProjectReadiness,
  portfolioProjectText,
  portfolioSkillCounts,
  portfolioStatusCounts,
  portfolioTechCounts,
  sortPortfolioProjects,
  topPortfolioProject,
  type PortfolioProject,
  type PortfolioProjectPriority,
  type PortfolioProjectStatus,
} from "@/lib/portfolio-projects";

export default function PortfolioPage() {
  const [projects, setProjects] = useLocalStorage<PortfolioProject[]>(LOCAL_STORAGE_KEYS.portfolioProjects, []); const [plannerTasks, setPlannerTasks] = useLocalStorage<PlannerTask[]>(LOCAL_STORAGE_KEYS.plannerTasks, []);
  const [name, setName] = useState(""); const [search, setSearch] = useState(""); const [statusFilter, setStatusFilter] = useState<PortfolioProjectStatus | "all">("all"); const [priorityFilter, setPriorityFilter] = useState<PortfolioProjectPriority | "all">("all"); const [showArchived, setShowArchived] = useState(false);
  const visibleProjects = sortPortfolioProjects(projects).filter((project) => { if (!showArchived && project.status === "archived") return false; if (statusFilter !== "all" && project.status !== statusFilter) return false; if (priorityFilter !== "all" && project.priority !== priorityFilter) return false; const query = search.trim().toLowerCase(); return !query || `${project.name} ${project.summary} ${project.targetRole} ${project.problem} ${project.solution} ${project.impact} ${project.techStack.join(" ")} ${project.skills.join(" ")} ${project.tags.join(" ")}`.toLowerCase().includes(query); });
  const statusRows = Object.entries(portfolioStatusCounts(visibleProjects)); const skillRows = Object.entries(portfolioSkillCounts(visibleProjects)).sort((a, b) => b[1] - a[1]); const techRows = Object.entries(portfolioTechCounts(visibleProjects)).sort((a, b) => b[1] - a[1]);
  const nextDeadline = nextPortfolioDeadline(projects);
  const topProject = topPortfolioProject(projects);
  const insightRows = [
    ["Due within 7 days", portfolioDueSoonCount(projects)],
    ["Average hours", portfolioAverageHours(projects)],
    ["Missing next action", portfolioMissingNextActionCount(projects)],
    ["Missing evidence", portfolioMissingEvidenceCount(projects)],
    ["Unscheduled", portfolioUnscheduledCount(projects)],
  ];
  function addProject() { if (!name.trim()) return; setProjects((current) => [createPortfolioProject(name), ...current]); setName(""); toast.success("Portfolio project added"); }
  function updateProject(id: string, patch: Partial<PortfolioProject>) { setProjects((current) => current.map((project) => project.id === id ? { ...project, ...patch, updatedAt: new Date().toISOString() } : project)); }
  function removeProject(id: string) { setProjects((current) => current.filter((project) => project.id !== id)); toast.success("Project deleted"); }
  function duplicateProject(project: PortfolioProject) { const now = new Date().toISOString(); setProjects((current) => [{ ...project, id: crypto.randomUUID(), name: `${project.name} copy`, createdAt: now, updatedAt: now }, ...current]); toast.success("Project duplicated"); }
  function addToPlanner(project: PortfolioProject) { const task: PlannerTask = { id: crypto.randomUUID(), title: project.nextAction || `Work on ${project.name}`, notes: `From portfolio tracker.${project.summary ? ` ${project.summary}` : ""}`, priority: project.priority, category: "learning", estimateMinutes: 60, resourceUrl: project.repositoryUrl || project.liveUrl, status: "todo", dueDate: project.targetDate, createdAt: new Date().toISOString(), completedAt: null, archived: false, tags: ["portfolio", ...project.skills.slice(0, 5)], recurrence: "none" }; setPlannerTasks([task, ...plannerTasks]); toast.success("Project action added to planner"); }
  async function importProjects(event: ChangeEvent<HTMLInputElement>) { const file = event.target.files?.[0]; event.target.value = ""; if (!file) return; try { const parsed = JSON.parse(await file.text()) as { projects?: PortfolioProject[] } | PortfolioProject[]; const incoming = Array.isArray(parsed) ? parsed : parsed.projects || []; setProjects((current) => mergePortfolioProjects(current, incoming)); toast.success(`${incoming.length} projects imported`); } catch { toast.error("Could not import projects"); } }
  function publishVisible() { const ids = new Set(visibleProjects.map((project) => project.id)); setProjects((current) => current.map((project) => ids.has(project.id) ? { ...project, status: "published", progress: 100, updatedAt: new Date().toISOString() } : project)); toast.success("Visible projects published"); }
  function archiveVisible() { const ids = new Set(visibleProjects.map((project) => project.id)); setProjects((current) => current.map((project) => ids.has(project.id) ? { ...project, status: "archived", updatedAt: new Date().toISOString() } : project)); toast.success("Visible projects archived"); }
  function restoreVisible() { const ids = new Set(visibleProjects.filter((project) => project.status === "archived").map((project) => project.id)); setProjects((current) => current.map((project) => ids.has(project.id) ? { ...project, status: "planning", updatedAt: new Date().toISOString() } : project)); toast.success("Visible archived projects restored"); }
  function clearArchived() { setProjects((current) => current.filter((project) => project.status !== "archived")); toast.success("Archived projects cleared"); }
  function resetFilters() { setSearch(""); setStatusFilter("all"); setPriorityFilter("all"); setShowArchived(false); }
  function prioritizeVisible() { const ids = new Set(visibleProjects.map((project) => project.id)); setProjects((current) => current.map((project) => ids.has(project.id) ? { ...project, priority: "high", updatedAt: new Date().toISOString() } : project)); toast.success("Visible projects prioritized"); }
  function featureVisible() { const ids = new Set(visibleProjects.map((project) => project.id)); setProjects((current) => current.map((project) => ids.has(project.id) ? { ...project, featured: true, updatedAt: new Date().toISOString() } : project)); toast.success("Visible projects featured"); }
  function unfeatureVisible() { const ids = new Set(visibleProjects.map((project) => project.id)); setProjects((current) => current.map((project) => ids.has(project.id) ? { ...project, featured: false, updatedAt: new Date().toISOString() } : project)); toast.success("Visible projects unfeatured"); }
  function startVisibleToday() { const ids = new Set(visibleProjects.filter((project) => ["idea", "planning"].includes(project.status)).map((project) => project.id)); const today = new Date().toISOString().slice(0, 10); setProjects((current) => current.map((project) => ids.has(project.id) ? { ...project, status: "building", startDate: project.startDate || today, updatedAt: new Date().toISOString() } : project)); toast.success("Visible projects started"); }
  function scheduleVisibleToday() { const ids = new Set(visibleProjects.map((project) => project.id)); const today = new Date().toISOString().slice(0, 10); setProjects((current) => current.map((project) => ids.has(project.id) ? { ...project, targetDate: today, updatedAt: new Date().toISOString() } : project)); toast.success("Visible projects scheduled today"); }
  function scheduleVisibleNextWeek() { const ids = new Set(visibleProjects.map((project) => project.id)); const date = new Date(); date.setDate(date.getDate() + 7); const targetDate = date.toISOString().slice(0, 10); setProjects((current) => current.map((project) => ids.has(project.id) ? { ...project, targetDate, updatedAt: new Date().toISOString() } : project)); toast.success("Visible projects scheduled next week"); }
  function clearVisibleDeadlines() { const ids = new Set(visibleProjects.filter((project) => project.targetDate).map((project) => project.id)); setProjects((current) => current.map((project) => ids.has(project.id) ? { ...project, targetDate: "", updatedAt: new Date().toISOString() } : project)); toast.success("Visible project deadlines cleared"); }
  return <div className="max-w-6xl space-y-6"><div><h2 className="text-xl font-semibold text-white">Portfolio Projects</h2><p className="mt-1 text-sm text-gray-400">Turn learning into visible proof of skill and impact.</p></div>
    <button onClick={publishVisible} disabled={!visibleProjects.length} className="rounded-lg border border-gray-700 px-3 py-2 text-sm text-gray-300 disabled:opacity-40">Publish visible</button>
    <button onClick={archiveVisible} disabled={!visibleProjects.length} className="rounded-lg border border-gray-700 px-3 py-2 text-sm text-gray-300 disabled:opacity-40">Archive visible</button>
    <button onClick={restoreVisible} disabled={!visibleProjects.some((project) => project.status === "archived")} className="rounded-lg border border-gray-700 px-3 py-2 text-sm text-gray-300 disabled:opacity-40">Restore visible</button>
    <button onClick={clearArchived} disabled={!projects.some((project) => project.status === "archived")} className="rounded-lg border border-gray-700 px-3 py-2 text-sm text-gray-300 disabled:opacity-40">Clear archived</button>
    <button onClick={resetFilters} className="rounded-lg border border-gray-700 px-3 py-2 text-sm text-gray-300">Reset filters</button>
    <button onClick={prioritizeVisible} disabled={!visibleProjects.some((project) => project.priority !== "high")} className="rounded-lg border border-gray-700 px-3 py-2 text-sm text-gray-300 disabled:opacity-40">Prioritize visible</button>
    <button onClick={featureVisible} disabled={!visibleProjects.length} className="rounded-lg border border-gray-700 px-3 py-2 text-sm text-gray-300 disabled:opacity-40">Feature visible</button>
    <button onClick={unfeatureVisible} disabled={!visibleProjects.some((project) => project.featured)} className="rounded-lg border border-gray-700 px-3 py-2 text-sm text-gray-300 disabled:opacity-40">Unfeature visible</button>
    <button onClick={startVisibleToday} disabled={!visibleProjects.some((project) => ["idea", "planning"].includes(project.status))} className="rounded-lg border border-gray-700 px-3 py-2 text-sm text-gray-300 disabled:opacity-40">Start visible</button>
    <button onClick={scheduleVisibleToday} disabled={!visibleProjects.length} className="rounded-lg border border-gray-700 px-3 py-2 text-sm text-gray-300 disabled:opacity-40">Due visible today</button>
    <button onClick={scheduleVisibleNextWeek} disabled={!visibleProjects.length} className="rounded-lg border border-gray-700 px-3 py-2 text-sm text-gray-300 disabled:opacity-40">Due visible next week</button>
    <button onClick={clearVisibleDeadlines} disabled={!visibleProjects.some((project) => project.targetDate)} className="rounded-lg border border-gray-700 px-3 py-2 text-sm text-gray-300 disabled:opacity-40">Clear visible deadlines</button>
    {nextDeadline && <div className="rounded-lg border border-blue-800/50 bg-blue-950/20 p-4 text-sm text-blue-100">Next deadline: <span className="font-semibold">{nextDeadline.name}</span> on {nextDeadline.targetDate}.</div>}
    {topProject && <div className="rounded-lg border border-emerald-800/50 bg-emerald-950/20 p-4 text-sm text-emerald-100">Strongest portfolio piece: <span className="font-semibold">{topProject.name}</span> at {topProject.progress}% progress.</div>}
    <div className="grid grid-cols-2 gap-3 lg:grid-cols-5">{[["Active", projects.filter((project) => !["published", "archived"].includes(project.status)).length], ["Published", projects.filter((project) => project.status === "published").length], ["Overdue", projects.filter((project) => isPortfolioProjectOverdue(project)).length], ["Hours", projects.reduce((sum, project) => sum + project.hoursSpent, 0)], ["Featured", projects.filter((project) => project.featured && project.status !== "archived").length]].map(([label, value]) => <div key={label} className="rounded-lg border border-gray-800 bg-gray-900 p-4"><div className="text-xs text-gray-500">{label}</div><div className="mt-1 text-xl font-bold text-white">{value}</div></div>)}</div>
    <div className="rounded-lg border border-gray-800 bg-gray-900 p-5"><h3 className="text-sm font-semibold text-white">Portfolio insights</h3><div className="mt-3 grid gap-2 sm:grid-cols-2 lg:grid-cols-5">{insightRows.map(([label, value]) => <div key={label} className="rounded-md bg-gray-800 px-3 py-2"><div className="text-xs text-gray-500">{label}</div><div className="mt-1 font-semibold text-white">{value}</div></div>)}</div></div>
    <div className="rounded-lg border border-gray-800 bg-gray-900 p-5"><div className="flex gap-2"><input value={name} onChange={(event) => setName(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter") addProject(); }} placeholder="Add a portfolio project" className="min-w-0 flex-1 rounded-lg border border-gray-700 bg-gray-800 px-4 py-2.5 text-sm text-white" /><button onClick={addProject} disabled={!name.trim()} className="inline-flex items-center gap-2 rounded-lg bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-40"><Plus className="h-4 w-4" />Add</button></div></div>
    <div className="flex flex-wrap gap-2"><select value={statusFilter} onChange={(event) => setStatusFilter(event.target.value as PortfolioProjectStatus | "all")} className="rounded-lg border border-gray-700 bg-gray-900 px-3 py-2.5 text-sm text-gray-300"><option value="all">All statuses</option>{PORTFOLIO_PROJECT_STATUSES.filter((option) => option.value !== "archived").map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select><select value={priorityFilter} onChange={(event) => setPriorityFilter(event.target.value as PortfolioProjectPriority | "all")} className="rounded-lg border border-gray-700 bg-gray-900 px-3 py-2.5 text-sm text-gray-300"><option value="all">All priorities</option>{PORTFOLIO_PROJECT_PRIORITIES.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select><label className="relative min-w-64 flex-1"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-gray-600" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search projects, skills, stack, or impact" className="w-full rounded-lg border border-gray-700 bg-gray-900 py-2.5 pl-9 pr-3 text-sm text-white" /></label><button onClick={() => setShowArchived((value) => !value)} className="rounded-lg border border-gray-700 px-3 text-sm text-gray-300">Archived</button><label className="inline-flex cursor-pointer items-center gap-2 rounded-lg border border-gray-700 px-3 text-sm text-gray-300"><Upload className="h-4 w-4" />Import<input type="file" accept=".json" onChange={importProjects} className="sr-only" /></label><CopyButton value={portfolioProjectText(visibleProjects) || "No projects yet"} label="Copy portfolio" /><button onClick={() => downloadJson("careerpilot-portfolio-projects.json", { projects })} className="inline-flex items-center gap-2 rounded-lg border border-gray-700 px-3 text-sm text-gray-300"><Download className="h-4 w-4" />JSON</button><button onClick={() => downloadCsv("careerpilot-portfolio-projects.csv", visibleProjects.map((project) => ({ name: project.name, status: project.status, priority: project.priority, target_role: project.targetRole, progress: project.progress, hours: project.hoursSpent, target_date: project.targetDate, skills: project.skills.join(", "), tech_stack: project.techStack.join(", "), live_url: project.liveUrl, repository_url: project.repositoryUrl })))} className="rounded-lg border border-gray-700 px-3 text-sm text-gray-300">CSV</button></div>
    {visibleProjects.length === 0 ? <div className="rounded-lg border border-dashed border-gray-700 py-14 text-center"><FolderKanban className="mx-auto h-8 w-8 text-gray-600" /><p className="mt-3 text-sm text-gray-400">No projects match this view.</p></div> : <div className="space-y-4">{visibleProjects.map((project) => <article key={project.id} className="rounded-lg border border-gray-800 bg-gray-900 p-5"><div className="flex gap-2"><button onClick={() => updateProject(project.id, { featured: !project.featured })} className={project.featured ? "text-amber-400" : "text-gray-600"}><Star className="h-4 w-4" fill={project.featured ? "currentColor" : "none"} /></button><input value={project.name} onChange={(event) => updateProject(project.id, { name: event.target.value })} className="min-w-0 flex-1 bg-transparent text-lg font-semibold text-white outline-none" /><button onClick={() => duplicateProject(project)} className="text-xs text-gray-400">Duplicate</button><button onClick={() => updateProject(project.id, { status: "archived" })} className="p-2 text-gray-400"><Archive className="h-4 w-4" /></button><button onClick={() => removeProject(project.id)} className="p-2 text-gray-500"><Trash2 className="h-4 w-4" /></button></div>
      <div className="mt-3 grid gap-3 sm:grid-cols-2 lg:grid-cols-5"><label className="text-xs text-gray-500">Status<select value={project.status} onChange={(event) => updateProject(project.id, { status: event.target.value as PortfolioProjectStatus })} className="mt-1 w-full rounded-md border border-gray-700 bg-gray-800 px-3 py-2 text-sm text-white">{PORTFOLIO_PROJECT_STATUSES.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label><label className="text-xs text-gray-500">Priority<select value={project.priority} onChange={(event) => updateProject(project.id, { priority: event.target.value as PortfolioProjectPriority })} className="mt-1 w-full rounded-md border border-gray-700 bg-gray-800 px-3 py-2 text-sm text-white">{PORTFOLIO_PROJECT_PRIORITIES.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label><label className="text-xs text-gray-500">Target role<input value={project.targetRole} onChange={(event) => updateProject(project.id, { targetRole: event.target.value })} className="mt-1 w-full rounded-md border border-gray-700 bg-gray-800 px-3 py-2 text-sm text-white" /></label><label className="text-xs text-gray-500">Start date<input type="date" value={project.startDate} onChange={(event) => updateProject(project.id, { startDate: event.target.value })} className="mt-1 w-full rounded-md border border-gray-700 bg-gray-800 px-3 py-2 text-sm text-white" /></label><label className="text-xs text-gray-500">Target date<input type="date" value={project.targetDate} onChange={(event) => updateProject(project.id, { targetDate: event.target.value })} className="mt-1 w-full rounded-md border border-gray-700 bg-gray-800 px-3 py-2 text-sm text-white" /></label></div>
      <div className="mt-3 grid gap-3 lg:grid-cols-2"><label className="text-xs text-gray-500">Summary<textarea rows={2} value={project.summary} onChange={(event) => updateProject(project.id, { summary: event.target.value })} className="mt-1 w-full rounded-md border border-gray-700 bg-gray-800 px-3 py-2 text-sm text-white" /></label><label className="text-xs text-gray-500">Problem<textarea rows={2} value={project.problem} onChange={(event) => updateProject(project.id, { problem: event.target.value })} className="mt-1 w-full rounded-md border border-gray-700 bg-gray-800 px-3 py-2 text-sm text-white" /></label><label className="text-xs text-gray-500">Solution<textarea rows={2} value={project.solution} onChange={(event) => updateProject(project.id, { solution: event.target.value })} className="mt-1 w-full rounded-md border border-gray-700 bg-gray-800 px-3 py-2 text-sm text-white" /></label><label className="text-xs text-gray-500">Impact<textarea rows={2} value={project.impact} onChange={(event) => updateProject(project.id, { impact: event.target.value })} className="mt-1 w-full rounded-md border border-gray-700 bg-gray-800 px-3 py-2 text-sm text-white" /></label><label className="text-xs text-gray-500">Skills<input value={project.skills.join(", ")} onChange={(event) => updateProject(project.id, { skills: event.target.value.split(",").map((value) => value.trim()).filter(Boolean) })} className="mt-1 w-full rounded-md border border-gray-700 bg-gray-800 px-3 py-2 text-sm text-white" /></label><label className="text-xs text-gray-500">Tech stack<input value={project.techStack.join(", ")} onChange={(event) => updateProject(project.id, { techStack: event.target.value.split(",").map((value) => value.trim()).filter(Boolean) })} className="mt-1 w-full rounded-md border border-gray-700 bg-gray-800 px-3 py-2 text-sm text-white" /></label><label className="text-xs text-gray-500">Repository URL<input type="url" value={project.repositoryUrl} onChange={(event) => updateProject(project.id, { repositoryUrl: event.target.value })} className="mt-1 w-full rounded-md border border-gray-700 bg-gray-800 px-3 py-2 text-sm text-white" /></label><label className="text-xs text-gray-500">Live URL<input type="url" value={project.liveUrl} onChange={(event) => updateProject(project.id, { liveUrl: event.target.value })} className="mt-1 w-full rounded-md border border-gray-700 bg-gray-800 px-3 py-2 text-sm text-white" /></label><label className="text-xs text-gray-500">Next action<input value={project.nextAction} onChange={(event) => updateProject(project.id, { nextAction: event.target.value })} className="mt-1 w-full rounded-md border border-gray-700 bg-gray-800 px-3 py-2 text-sm text-white" /></label><label className="text-xs text-gray-500">Tags<input value={project.tags.join(", ")} onChange={(event) => updateProject(project.id, { tags: event.target.value.split(",").map((value) => value.trim()).filter(Boolean) })} className="mt-1 w-full rounded-md border border-gray-700 bg-gray-800 px-3 py-2 text-sm text-white" /></label></div>
      <div className="mt-3 grid gap-3 lg:grid-cols-2"><label className="text-xs text-gray-500">Case study URL<input type="url" value={project.caseStudyUrl} onChange={(event) => updateProject(project.id, { caseStudyUrl: event.target.value })} className="mt-1 w-full rounded-md border border-gray-700 bg-gray-800 px-3 py-2 text-sm text-white" /></label><label className="text-xs text-gray-500">Project image URL<input type="url" value={project.imageUrl} onChange={(event) => updateProject(project.id, { imageUrl: event.target.value })} className="mt-1 w-full rounded-md border border-gray-700 bg-gray-800 px-3 py-2 text-sm text-white" /></label></div>
      <label className="mt-3 block text-xs text-gray-500">Notes<textarea rows={3} value={project.notes} onChange={(event) => updateProject(project.id, { notes: event.target.value })} className="mt-1 w-full rounded-md border border-gray-700 bg-gray-800 px-3 py-2 text-sm text-white" /></label>
      <div className="mt-3 text-xs text-gray-500">Portfolio readiness <span className="font-semibold text-blue-300">{portfolioProjectReadiness(project)}%</span></div>
      <div className="mt-4 flex flex-wrap items-center gap-4 border-t border-gray-800 pt-4"><label className="text-xs text-gray-500">Progress <input type="range" min="0" max="100" value={project.progress} onChange={(event) => updateProject(project.id, { progress: Number(event.target.value) })} /><span className="text-white"> {project.progress}%</span></label><label className="text-xs text-gray-500">Hours <input type="number" min="0" value={project.hoursSpent} onChange={(event) => updateProject(project.id, { hoursSpent: Number(event.target.value) })} className="ml-2 w-20 rounded border border-gray-700 bg-gray-800 px-2 py-1 text-white" /></label><label className="text-xs text-gray-500">Milestones <input type="number" min="0" value={project.milestoneCount} onChange={(event) => updateProject(project.id, { milestoneCount: Number(event.target.value) })} className="ml-2 w-16 rounded border border-gray-700 bg-gray-800 px-2 py-1 text-white" /></label><label className="text-xs text-gray-500">Completed <input type="number" min="0" max={project.milestoneCount || undefined} value={project.completedMilestones} onChange={(event) => updateProject(project.id, { completedMilestones: Number(event.target.value) })} className="ml-2 w-16 rounded border border-gray-700 bg-gray-800 px-2 py-1 text-white" /></label>{isPortfolioProjectOverdue(project) && <span className="rounded-full bg-amber-950 px-2.5 py-1 text-xs text-amber-300">Overdue</span>}{(project.liveUrl || project.repositoryUrl) && <a href={project.liveUrl || project.repositoryUrl} target="_blank" rel="noreferrer" className="text-gray-400"><ExternalLink className="h-4 w-4" /></a>}<button onClick={() => addToPlanner(project)} className="ml-auto rounded-md border border-gray-700 px-3 py-1.5 text-xs text-gray-300">Add to planner</button></div></article>)}</div>}
    {(statusRows.some(([, count]) => count) || skillRows.length || techRows.length) && <div className="grid gap-4 md:grid-cols-3"><div className="rounded-lg border border-gray-800 bg-gray-900 p-5"><h3 className="text-sm font-semibold text-white">Pipeline</h3>{statusRows.filter(([, count]) => count).map(([key, count]) => <div key={key} className="mt-2 flex justify-between text-sm text-gray-400"><span>{PORTFOLIO_PROJECT_STATUSES.find((option) => option.value === key)?.label}</span><span>{count}</span></div>)}</div><div className="rounded-lg border border-gray-800 bg-gray-900 p-5"><h3 className="text-sm font-semibold text-white">Skill coverage</h3><div className="mt-3 flex flex-wrap gap-2">{skillRows.slice(0, 12).map(([skill, count]) => <span key={skill} className="rounded-full bg-gray-800 px-2.5 py-1 text-xs text-gray-300">{skill} · {count}</span>)}</div></div><div className="rounded-lg border border-gray-800 bg-gray-900 p-5"><h3 className="text-sm font-semibold text-white">Technology</h3><div className="mt-3 flex flex-wrap gap-2">{techRows.slice(0, 12).map(([tech, count]) => <span key={tech} className="rounded-full bg-gray-800 px-2.5 py-1 text-xs text-gray-300">{tech} · {count}</span>)}</div></div></div>}
  </div>;
}
